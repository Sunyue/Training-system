<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sap.dao.CourseDao">
<!--设置domain类和数据库中表的字段一一对应，注意数据库字段和domain类中的字段名称不致，此处一定要！-->
    <resultMap id="BaseResultMap" type="com.sap.domain.Course">
        <id column="courseid" property="courseId" jdbcType="INTEGER" />
        <result column="coursename" property="courseName" jdbcType="CHAR" />
        <result column="description" property="description" jdbcType="CHAR" />
    </resultMap>
    <!-- 查询单条记录 -->
    <select id="selectCourseById" parameterType="int" resultType="com.sap.domain.Course">
        SELECT * FROM courseinfo WHERE courseid = #{courseId}
    </select>
    
    <!-- 查询指定Chain的全部Course记录 -->
    <select id="selectCourseByChain" parameterType="int" resultType="com.sap.domain.Course">
        SELECT courseinfo.* FROM courseinfo, chaininfo, chaincourse 
         WHERE chaininfo.chainid = chaincourse.chainid
           AND courseinfo.courseid = chaincourse.courseid
           AND chaininfo.chainid = #{chainId}           
    </select>
    
    <!-- 查询全部Chain记录 -->
    <select id="selectAllChain" resultType="com.sap.domain.Course">
        SELECT * FROM chaininfo
    </select>
    
</mapper>
